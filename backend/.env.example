ENVIRONMENT=development
MONGO_URI=mongodb://localhost:27017
MONGO_DB=helpdesk_casma
# python -c "import secrets;print(secrets.token_urlsafe(48))"
JWT_SECRET=CAMBIAR_POR_VALOR_ALEATORIO_DE_48_CARACTERES_O_MAS________
# python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())"
FIELD_ENCRYPTION_KEY=CAMBIAR_POR_CLAVE_FERNET_GENERADA=========
COOKIE_SECURE=false
CORS_ORIGINS=
# Redes desde donde pueden ingresar las oficinas (vacío = sin restricción)
OFFICE_ALLOWED_NETWORKS=10.0.0.0/8,192.168.0.0/16,127.0.0.1/32
REQUIRE_DEVICE_APPROVAL=true
PUBLIC_BASE_URL=http://localhost:5173
UPLOAD_DIR=./storage/uploads
MODEL_DIR=./storage/models
BACKUP_DIR=./storage/backups
BACKUP_RETENTION_DAYS=14
RUN_SCHEDULER=true
# tfidf | sentence-transformers (requiere requirements-dl.txt)
AI_EMBEDDINGS=tfidf
AI_AUTO_ASSIGN_URGENT=false
